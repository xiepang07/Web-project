spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/mybatisplustest
    username: root
    password: 20030711
  main:
    banner-mode: off

mybatis-plus:
  global-config:
    banner: false
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.example.domain

server:
  port: 8085

logging:
  level:
    root: info
    com: debug